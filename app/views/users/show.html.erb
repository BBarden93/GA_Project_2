
<h1> <%= @user.name %> </h1>

<h2><%= @user.about %></h2>
<%= image_tag(@user.image) %>



<%= link_to("Destroy User", "/users/#{@user.id}", {method: 'delete', data: {confirm: "This one...really?"}}) %>
